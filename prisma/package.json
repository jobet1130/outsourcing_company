{
  "name": "prisma-client-py",
  "version": "1.0.0",
  "description": "Prisma client setup for Python",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^4.16.2"
  },
  "devDependencies": {
    "prisma": "^4.16.2"
  }
}
